spring:
  application:
    name: product

  datasource:
    url: jdbc:postgresql://localhost:5432/ms-product
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8082
